import styled from "styled-components";

export const PainelBox = styled.article`
    box-sizing: border-box;
    padding: 20px;
    display: flex;
    flex-wrap: wrap;

    input[type="range"]{
        width: 300px;
    }
`;